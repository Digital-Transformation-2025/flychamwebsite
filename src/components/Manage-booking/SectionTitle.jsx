import React from 'react';

const SectionTitle = ({ children }) => (
  <h2 className="text-xl sm:text-2xl md:text-[28px] font-semibold text-[#5F5F5C] ">
    {children}
  </h2>
);

export default SectionTitle;
